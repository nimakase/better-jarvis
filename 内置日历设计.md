# 内置日历设计（方案 C · 时间真源）

> 目标：日历成为 jarvis 的**时间底座 / 单一事实来源**，所有带日期的功能都汇到它上面，
> 并对全局可用（注入模型 + 到点提醒 + 界面）。扩展原则：新功能要上日历＝注册一个来源，
> 不碰日历核心。与现有"注册表家族"（intel_cards / reports / workflows）同构。

---

## 1. 数据模型 —— 一张可扩展的事件表

`core/calendar.py`，存进现有 `memory.db`（独立表，沿用 history/profile/vault 模式）。

表 `calendar_events`：

| 字段 | 说明 |
|---|---|
| `id` | uuid |
| `title` | 标题 |
| `start` | ISO 日期或日期时间 |
| `end` | 可空；区间事件（如休假）用 |
| `all_day` | 是否全天 |
| `kind` | 事件种类：`event`(用户) / `rest`(休假) / `reminder`(提醒) / …可扩展 |
| `rrule` | 可空；重复规则（RRULE 子集），周期事件第一类支持 |
| `notify` | 可空；提醒时机（如开始前 N 分钟 / 当天 09:00） |
| `notify_state` | 已推送标记，防重复 |
| `source` | `user` / `system`；谁建的（派生项不入表，见 §2） |
| `meta` | JSON 扩展袋：种类专属字段（休假事由、关联证件 alias、关联定时任务名…） |
| `created_at` / `updated_at` | |

**扩展性两根支柱**：
- **`kind` + `meta` JSON 袋**：新增事件种类**零 schema 迁移**。
- **`rrule` 字段**：重复事件是第一类公民（每周例会等），不外包给 scheduler。

---

## 2. 真源 vs 派生 —— 边界划清

- **真源（存进 `calendar_events`，日历拥有）**：用户事件、休假 `rest`、一次性提醒 `reminder`。
- **派生（不入表，读时由来源 provider 现算）**：证件/保单到期、定时任务下次运行、工作流/报告周期、档案关键日期。它们仍住在各自功能里，日历只聚合成只读视图。

**来源注册表**（扩展枢纽，对标 `intel_cards.register_card`）：
```
calendar.register_source(name, fn)   # fn(start, end) -> [CalEntry...]
```
统一读 API `calendar.agenda(start, end)`：合并「表内事件（展开 rrule）」+「各 provider 的派生条目」→ 一条排好序的时间轴。
新功能要上日历 = 在自己模块里 `register_source(...)`，日历核心不动。

内置 provider（各功能各自注册）：credentials（到期）、doc_vault（保单到期）、scheduler（下次运行）、workflow_registry（周期产出）、profile（关键日期）。

---

## 3. 休假迁移 —— 方案 C 的核心整合（带向后兼容）

现状：`delivery.add_rest_period` 写 `delivery_state.json` 的 `rest_periods`，`delivery.is_paused()` 读它。

目标：**休假＝日历事件 `kind=rest`**；`delivery.is_paused()` 改读日历（查覆盖今天的 rest 事件）。

落地：
1. 一次性迁移：启动时把 `delivery_state.rest_periods` 旧数据搬成日历 rest 事件（幂等，迁移后置空旧字段）。
2. `delivery_rest` 工具（add/clear/confirm）改为操作日历 rest 事件——对模型/用户行为不变，底层换成日历。
3. **边界**：`delivery_pause`（某轨 report/prospect 暂停，是开关不是日期）**留在 delivery**，不迁。清晰切分：休假（影响两轨的日期区间）＝日历；轨道开关＝delivery。
4. 闭环：休假在日历 → `delivery.is_paused` 命中 → 日报/潜客推送被压制（与现有行为一致）。

---

## 4. 环境感知 —— 让模型每轮都懂日程

`calendar.build_block()` → "近期日程（未来 N 天）" 注入 system prompt，和 `profile.build_block()` 并列由 `controller._build_system_prompt` 每轮拼入。含：将到事件 + 休假 + 临近到期 + 今日定时。有条数/字数上限，保持小而精。

效果：任何对话都自带日程意识——"帮我安排下周"知道已有占用；"这保单要续吗"知道 5 天后到期。这是日历成为"顶层"的关键。

---

## 5. 通知 —— 复用现有 webpush

`scheduler` 加一个巡检 job（**每 1 分钟**）：扫日历里 `notify` 到点且 `notify_state` 未发的事件 → `delivery.deliver(track="reminder", …)` → webpush 推送 → 标记已发。
零新基建：webpush 渠道已在 `web/push.py` 注册；rest 事件本就压制投递，闭环完整。

**心跳为纯机械、零 AI**：扫表 + 布尔翻转，不经 controller、不进对话循环、不碰 system prompt，与 scheduler 其余 job 同构。提醒文案阶段 1 用模板拼接（标题 + 时间 + 「即将开始/今日到期」）；将来若想要更聪明的措辞，再在投递点挂可选的轻量生成，默认关。

**间隔与可靠性**：间隔 1 分钟（M 系列对扫表零压力，精度也贴点）。真正的可靠性约束不是算力而是**进程是否常驻**——运行设备需设为熄屏不休眠且长期通电（当前 M5 MacBook Air，未来迁 M4 Mac mini 常驻更稳）。另设**补发窗口**：醒来/重启后若发现触发时刻已过但未发，窗口内（建议 2 小时）补推一次，超窗只标记不打扰，兜睡眠/断电的洞。

---

## 6. 模型工具（`calendar` 组，配合渐进披露）

- `create_event(title, start, end?, all_day?, rrule?, notify?, kind?)`
- `agenda(start?, end?)` —— 读统一时间轴（事件 + 派生），默认未来 7 天
- `update_event(id, …)` / `delete_event(id)`
- 休假沿用 `delivery_rest`（底层改写日历），不另起炉灶。

`agenda` 可考虑进 `CORE_TOOL_NAMES`（任意对话都可能查日程）；写类工具按需 `load_tools(calendar)`。

---

## 7. 界面

- 抽屉新增「日历」面板（月视图 / 议程视图），REST `/api/calendar`（list/create/update/delete/agenda）。
- 情报台「近期日程」卡（`intel_cards.register_card`，action/monitor 层），一眼看将到事项。

---

## 8. AI 增强层与触点配合（进阶设计）

> 目标：让 AI 在提醒触发后「介入得更聪明」，同时厘清**日历界面**与 **AI 对话**两个触点的边界，使其配合而不重合。

**铁律：真源唯一，触点皆客户端。** `calendar_events` 是唯一事实来源；界面、AI、心跳、provider 都只是它的读写客户端，谁都不另存一份。这是「两个触点不打架」的根本——它们看到的是同一份数据。

**三者职责切分（各做一件别人干不了的事）：**
- **界面**：展示 + 精确操作（看月视图、点建事件、拖动改时间）。强项：快、确定、零 token、可视。适合「我知道要干嘛」。
- **AI 对话**：理解 + 生成 + 判断（"帮我排下周""这保单要续吗"）。适合「模糊意图 / 跨数据 / 需要拿主意」。
- **真源**：只负责存。

**提醒分级——AI 是旁路增强，不上心跳关键路径：**
- **L0 · 模板（零 AI，默认）**：绝大多数提醒。"10:00 客户电话 即将开始"。即时、免费、稳。
- **L1 · AI 增强（按需，异步旁路）**：仅 `meta.ai_assist=true` 的事件才走。心跳仍纯机械只负责「到点」；到点后把事件丢进**异步任务**，AI 跑完再推送，心跳不等待、不变慢。例：「客户电话 30 分钟后 —— 上次报价卡住，HubSpot 这单还停着，要我拉要点吗？」

**推送 = 进入 AI 对话的入口（一招解决两个顾虑）：** 不让 AI 在推送里把活干完（没人看、烧 token），而是推送做钩子——
```
心跳到点(机械) ─▶ 推送(L0 模板 / L1 一句话钩子) ─▶ 用户点一下 ─▶ 带事件上下文进入 AI 对话(深度介入)
```
「何时介入」自然分两段：触发时给便宜钩子，用户响应后才做值得的深度介入。

**界面 ↔ AI 双向嵌入，仍不重合：**
- 界面里嵌 AI：每个事件挂「问 AI / 交给 AI 处理」按钮 → 以该事件为上下文开对话。
- AI 里嵌界面：AI 涉及日程的回答渲染**小日历卡片**（复用 §7 的 `intel_cards`），可在对话里直接看/确认。
- 边界：界面管「看与点」，AI 管「懂与写」，真源管「存」，互相调用但不抢活。

---

## 9. 建议的分阶段落地（每阶段可独立交付/回滚）

1. **核心存储 + CRUD + 工具**：`core/calendar.py`（表 + 事件 CRUD + rrule 展开）、`connectors/calendar_tools.py`（create/agenda/update/delete）、REST。先有个能用的日历。
2. **环境感知注入**：`build_block()` 接入 system prompt。模型开始懂日程。
3. **来源注册表 + 内置 provider**：`register_source` + 证件/保单到期、定时任务、周期产出、档案关键日期五个 provider。日历变聚合视图。
4. **休假迁移**：rest → 日历事件，`delivery.is_paused` 改读日历，一次性迁移 + `delivery_rest` 改写。
5. **通知**：scheduler 巡检 + webpush 提醒。
6. **界面**：抽屉日历面板 + 情报卡 + 推送可点击进对话（§8）。
7. **AI 增强层（可选）**：L1 异步旁路 + `meta.ai_assist` 标记 + 界面/AI 双向嵌入（§8）。默认关，纯加法，不动心跳与投递核心。

阶段 1–3 是纯加法、零风险；阶段 4 动现有投递逻辑，需回归测试；阶段 7 为可选增强，默认关、零风险。

---

## 10. 待定（实现前确认）

- **重复规则范围（已定稿 2026-06-23）**：采用「设计子集 + 三项低成本扩展」。
  - 支持字段：`FREQ ∈ {DAILY, WEEKLY, MONTHLY, YEARLY}` + `INTERVAL` + `BYDAY`（含序号形式如 `-1FR` 每月最后一个周五 / `2MO` 每月第二个周一）+ `BYMONTHDAY` + `BYMONTH` + `COUNT` 或 `UNTIL`。
  - 比原始子集多收 **YEARLY / BYMONTH / BYDAY 序号**：覆盖「年度证件年审」「每月最后一个工作日对账」两个真实场景，`dateutil.rrule` 原生支持，实现成本近零。
  - 明确砍掉（本场景用不到）：`BYSETPOS` / `BYWEEKNO` / `BYYEARDAY` / `BYHOUR|MINUTE|SECOND`。
  - **EXDATE / 改单次 vs 改系列**：阶段 1 **不做**。临时改某次直接删后补一条单次事件即可，避免核心 CRUD 复杂化；待界面阶段再评估。
  - **性能约束**：展开必须用区间查询 `dateutil.rrule.between(start, end)`，**禁止全量展开后再过滤**——这是无限重复规则唯一的性能隐患点。
- **提醒粒度（已定稿 2026-06-23）**：两种 notify 模式——`before`（挂 `start`，提前 N 分钟，用于有钟点事件）+ `at`（绝对时刻，用于全天/区间事件）。全天事件未指定时默认 **当天 09:00** 提醒。`notify` 存 JSON 结构（非裸字段），阶段 1 单条提醒，给将来「多重提醒」留口子、不迁数据。巡检间隔 1 分钟 + 补发窗口 2 小时（详见 §5）。休假压制复用现有 delivery 逻辑，无需新做。
- **轨道 pause-until 是否也进日历（已定稿 2026-06-23）**：**不进，留 delivery。** 理由：pause-until 本质是「带恢复日期的轨道开关」——只影响单条轨（开关属性为主），日期属性为次，与「影响全轨的休假」性质不同，不应混入 `kind=rest`。数据继续住在 delivery；若以后想「暂停到某日」也在日历上可见，加一个**只读 provider** 聚合显示即可（派生项，不迁数据，避免两边同步）。守住 §2 真源/派生、§3 休假/开关两条边界。
