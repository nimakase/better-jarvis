# 贾维斯公网访问部署指南（Cloudflare Tunnel + Access + PWA）

把本机 `127.0.0.1:8000` 安全地暴露到公网，在手机/其他设备上通过浏览器或「添加到主屏幕」的 PWA 访问。

> ⚠️ **安全第一**：贾维斯能执行工具代码、读写本机文件、还有证件保险箱。**绝不能裸暴露**。本指南的第 3 步（Cloudflare Access）是必做项，它在入口加一道登录验证，只有你本人能进。

代码侧已经准备好：WebSocket 已支持 `wss`（HTTPS 自动切换），PWA 的 manifest / service worker / 图标也已就绪。下面只剩你在自己机器和 Cloudflare 账号上的操作。

---

## 前置条件

1. 一个 Cloudflare 账号（免费）。
2. 一个托管在 Cloudflare 上的域名（免费方案即可）。下面假设你的域名是 `example.com`，最终访问地址是 `jarvis.example.com`。
3. 贾维斯正常运行：`python main.py`，本机能打开 `http://localhost:8000`。

---

## 第 1 步：安装 cloudflared（Windows）

用 winget 最简单（管理员 PowerShell）：

```powershell
winget install --id Cloudflare.cloudflared
```

或从 https://github.com/cloudflare/cloudflared/releases 下载 `cloudflared-windows-amd64.exe`，重命名为 `cloudflared.exe` 放到 PATH 里。

验证：

```powershell
cloudflared --version
```

### （可选）先快速试一下——不带认证，仅临时测试

```powershell
cloudflared tunnel --url http://localhost:8000
```

会得到一个随机的 `https://xxxx.trycloudflare.com` 地址。**这个地址没有任何登录保护，任何拿到链接的人都能控制你的贾维斯**，只用于验证「隧道能不能通」，测完立刻 Ctrl+C 关掉，别长期用。正式使用走下面的命名隧道 + Access。

---

## 第 2 步：创建命名隧道并绑定域名

```powershell
# 1) 登录（浏览器里选择你的域名授权）
cloudflared tunnel login

# 2) 创建隧道（名字随意，这里叫 jarvis）
cloudflared tunnel create jarvis

# 3) 把子域名解析到这个隧道
cloudflared tunnel route dns jarvis jarvis.example.com
```

创建后会生成一个隧道凭证文件（`.json`），路径形如
`C:\Users\<你的用户名>\.cloudflared\<隧道ID>.json`，记下这个隧道 ID。

新建配置文件 `C:\Users\<你的用户名>\.cloudflared\config.yml`：

```yaml
tunnel: <隧道ID>
credentials-file: C:\Users\<你的用户名>\.cloudflared\<隧道ID>.json

ingress:
  - hostname: jarvis.example.com
    service: http://localhost:8000
  - service: http_status:404
```

运行隧道：

```powershell
cloudflared tunnel run jarvis
```

现在 `https://jarvis.example.com` 应该能打开贾维斯了（WebSocket 会自动走 `wss`，Cloudflare 原生支持，无需额外配置）。

### 让它开机自启（作为 Windows 服务）

```powershell
# 管理员 PowerShell
cloudflared service install
```

之后隧道会作为后台服务常驻。贾维斯本身（`python main.py`）也要保持运行——可以用任务计划程序设成开机启动。

---

## 第 3 步：加认证（Cloudflare Access）—— 必做

这一步给入口加登录门，只放行你本人。

1. 打开 Cloudflare 控制台 → **Zero Trust**（首次进入会让你起一个团队名，免费方案选 Free）。
2. 左侧 **Access → Applications → Add an application → Self-hosted**。
3. 配置：
   - **Application name**：贾维斯
   - **Session Duration**：按需，比如 24 小时或 1 周
   - **Application domain**：`jarvis.example.com`
4. 下一步 **Add policy**：
   - **Policy name**：仅本人
   - **Action**：Allow
   - **Include** → 选 **Emails** → 填你的邮箱（如 `你的邮箱@example.com`）
5. 保存。

之后访问 `https://jarvis.example.com` 会先跳到 Cloudflare 登录页，输入邮箱 → 收一次性验证码（One-time PIN）→ 验证通过才进得去。手机上的 PWA 同样走这套登录，登录后凭证会记住，平时不用反复输。

> 想更省事可以加一个 **Service Token** 或换成固定密码方案，但「邮箱 + 一次性验证码」对个人用最简单也够安全。

---

## 第 4 步：在手机上安装 PWA

1. 用安卓 **Chrome** 打开 `https://jarvis.example.com`，完成 Access 登录。
2. 右上角菜单 **⋮ → 安装应用 / 添加到主屏幕**（Add to Home screen）。
3. 主屏幕会出现「贾维斯」图标，点开就是全屏、无浏览器地址栏的独立窗口，体验接近原生 App。

> iOS 用 Safari：分享菜单 → 添加到主屏幕。

如果没出现「安装」选项，检查：必须是 HTTPS（Cloudflare 已满足）、`manifest.webmanifest` 和 `sw.js` 能正常访问（直接在浏览器打开 `https://jarvis.example.com/sw.js` 应能看到内容）。

---

## 排错速查

| 现象 | 排查 |
|---|---|
| 打开就转圈 / 连不上 | 确认本机 `python main.py` 在跑、`cloudflared tunnel run` 在跑 |
| 对话发不出去 | 多半是 WebSocket。打开浏览器开发者工具 Console 看有没有 `wss` 报错；确认走的是 `https://`（不是 http） |
| 没有「安装应用」选项 | 检查是 HTTPS、`/manifest.webmanifest` 与 `/sw.js` 可访问、用的是 Chrome |
| 任何人都能打开 | **Access 没配好**！回到第 3 步确认 Application domain 和 Policy 都对上了 |

---

## 安全提醒

- `main.py` 保持绑定 `127.0.0.1`（当前就是），不要改成 `0.0.0.0`——cloudflared 在本机连 localhost 就够了，这样即使没有隧道，外网也碰不到 8000 端口。
- 证件保险箱的真实值只在「本机 → 你的浏览器」之间传输，不进对话历史。但公网暴露后，**谁能登录谁就能看**，所以 Access 的邮箱白名单务必只留你自己。
- 定期在 Zero Trust → Logs 里看看有没有异常登录。
