# 分发与部署

贾维斯是本地优先的个人助理，默认只监听 `127.0.0.1`。下面三种分发方式按从简到繁排列。

## 配置（环境变量 / .env）

配置由 `config.py` 的 `Settings`（pydantic-settings）统一管理，可用环境变量或项目根目录的 `.env` 文件提供。常用项：

| 变量 | 说明 | 默认 |
|------|------|------|
| `OPENROUTER_API_KEY` | OpenRouter 密钥（必填，否则模型不可用） | 空 |
| `MEMORY_ENCRYPTION_KEY` | 记忆/保险箱加密密钥；本机留空会自动生成并存系统钥匙串 | 空 |
| `JARVIS_HOST` | 监听地址；容器/公网部署改 `0.0.0.0` | `127.0.0.1` |
| `JARVIS_PORT` | 监听端口 | `8000` |

复制 `.env.example` 为 `.env` 填入即可。所有项都有合理默认，缺失不会导致启动崩溃（相关功能在调用时才提示）。

## 方式一：源码直接运行（开发/本机日常）

```bash
pip install -e .          # 安装依赖与 jarvis 命令
python main.py            # 或：jarvis
# 打开 http://127.0.0.1:8000
```

可选能力按需装：`pip install -e ".[documents]"`（文档解析）、`pip install -e ".[ocr]"`（证件 OCR）、`pip install -e ".[all]"`（全部）。

## 方式二：构建 wheel 分发

```bash
pip install build
python -m build --wheel            # 产出 dist/jarvis-0.1.0-py3-none-any.whl
pip install dist/jarvis-0.1.0-py3-none-any.whl
```

注意：wheel 不含 `frontend/` 静态资源（仅打包 Python 包）。若用 wheel 安装并以 `jarvis` 命令启动，需另把 `frontend/` 放到 `main.py` 同级目录，或直接用「源码运行 / 容器」方式。把前端作为 package-data 一并分发可作为后续改进。

## 方式三：Docker

```bash
docker build -t jarvis .
docker run -p 8000:8000 \
  -e OPENROUTER_API_KEY=sk-... \
  -e MEMORY_ENCRYPTION_KEY=<32字节 base64 的 Fernet 密钥> \
  -v jarvis_data:/root/.jarvis \
  jarvis
```

要点：

- 镜像内已设 `JARVIS_HOST=0.0.0.0`，所以容器对外可达；宿主通过 `-p` 映射访问。
- 容器没有系统钥匙串，**必须显式提供 `MEMORY_ENCRYPTION_KEY`**，否则首次写入记忆/保险箱会失败。生成方法：
  `python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())"`
- 用具名卷挂载 `/root/.jarvis` 持久化记忆库与证件保险箱，否则容器重建后数据丢失。
- 镜像默认只装核心依赖；需要文档解析/OCR 时，在 `Dockerfile` 的安装行改为 `pip install -e ".[all]"`。

## 安全提醒

默认绑定 `127.0.0.1` 是有意为之：服务无鉴权，证件保险箱等敏感数据只应在本机访问。若改 `JARVIS_HOST=0.0.0.0` 暴露到网络，请自行在前面加反向代理 + 鉴权，并参考 `部署-公网访问.md`。
