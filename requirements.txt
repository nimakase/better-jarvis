openai>=1.35.0
fastapi>=0.111.0
uvicorn>=0.30.0
websockets>=12.0
httpx>=0.27.0
cryptography>=42.0.0
keyring>=25.0.0
python-dotenv>=1.0.0
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
apscheduler>=3.10.0
python-multipart>=0.0.9
rapidocr-onnxruntime>=1.3.0
pydantic-settings>=2.0.0
pywebpush>=1.14.0
python-dateutil>=2.8.0   # 内置日历 rrule 子集展开（core/calendar.py）
pandas>=2.0.0            # prospecting/hubspot_worker.py 顶层依赖（HubSpot 匹配流程）
numpy>=1.24.0           # core/embedding.py · core/episodic.py 顶层依赖（向量召回）
fastembed>=0.3.0        # L4 情节记忆本地嵌入（可选；未装则自动降级为本地哈希向量，仍可用）
# opencv-python / numpy 由 connectors/cred_ocr.py 直接 import；随 rapidocr-onnxruntime 传递安装，如需单独使用 OCR 模块可显式装：
# opencv-python>=4.5.1.48
# numpy>=1.19.5
# 可选：prospecting 流程还需 playwright（见下），market_intel_report PDF 日报同样需要。装后还需：playwright install chromium
# playwright>=1.40.0
