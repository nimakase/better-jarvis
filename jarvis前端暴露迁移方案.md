# Jarvis 前端暴露方案:从 Cloudflare 迁移到自建链路

> 目标:替换当前不稳定的 Cloudflare Tunnel(国内访问被 QoS 抖动),在保持 **jarvis 本地部署不变** 的前提下,搭一条国内可达、稳定、免备案、且安全可控的对外暴露链路;并为未来"自建安卓 App + 设备绑定"留出演进路径。
>
> 约束:jarvis 必须跑在本地机器(依赖本地环境),不能整体搬到云上 → 只能走"云服务器中转 + 反向代理"的隧道模式。

---

## 1. 方案总览

```
用户设备(大陆)
   │  HTTPS + mTLS(只有授权设备能握手成功)
   ▼
阿里云大陆服务器(公网 IP)
   ├─ Caddy   : 反向代理 + 自动 HTTPS(DNS 验证签证书)+ mTLS 验证客户端证书
   └─ frps    : frp 服务端,非标端口中转(如 7000 / :8443)
   │
   │  frp 隧道(全程大陆境内)
   ▼
本地机器(jarvis 所在)
   └─ frpc    : frp 客户端,把 jarvis 的本地端口转上去
   └─ jarvis  : 维持现状,监听 127.0.0.1:<本地端口>
```

把 `cloudflared` 换成 `frpc`,本地机器照样不需要公网 IP。区别是中转节点从 Cloudflare 的边缘换成你自己掌控的阿里云服务器。

---

## 2. 为什么选"大陆服务器 + 非标端口",且不用备案

ICP 备案只在**一种特定形态**下被强制:**域名 + 80/443 端口 + 网站服务 + 解析到大陆 IP**,四个条件同时满足才触发。阿里云的拦截机制实际只封"未备案域名解析到大陆 IP 的 80/443"。

只要避开这个组合,就天然免备案——这正是各类远程控制 / 跳板方案的做法:

- **用非标准端口**(如 `:8443`、`:8080`),不走 80/443;或
- **直接用 IP 访问**,根本不绑域名;或
- **纯 TCP 隧道**(frp / SSH / RDP),本就不是"网站"形态。

因此 **大陆服务器 + frp + 非标端口** 可以同时拿到:免备案、全程大陆境内(三大运营商直连,延迟低于香港、晚高峰不绕日本)、本地部署不变。这比香港方案更优——香港唯一的优势是免备案,而大陆用非标端口同样能免。

**代价(均可接受):**

1. 访问地址带端口号,如 `https://jv.example.com:8443`,没有干净的 `https://域名`。自用无所谓。
2. HTTPS 证书签发不能走默认的 HTTP-01 验证(走 80,被封),改用 **DNS-01 验证**(Caddy / acme.sh 在阿里云 DNS 上自动加一条 TXT 记录),照样签正规证书。

---

## 3. 域名:现在是"可选"

非标端口方案下域名不再是必需。两种都行:

- **纯 IP,不要域名**:`https://<公网IP>:8443`。最省一步,但 HTTPS 难搞(免费证书基本只签域名,纯 IP 只能自签 → 浏览器警告,或走明文)。建议买固定公网 IP,避免实例变更后 IP 改变。
- **留一个便宜域名(推荐)**:只做解析、不备案。域名此时唯一的价值 = **好记的名字 + 能签正规 HTTPS 证书**。每年几十块,让 HTTPS 和后续 mTLS 都更顺,不建议为省这点钱牺牲体验。

---

## 4. 安全分层:两层职责,别混

迁出 Cloudflare 后会丢掉 **Cloudflare Access(Zero Trust)** 那层"认证后才放行"的保护。必须自己补回来。安全分两层:

### 4.1 网络层 —— 交给阿里云(基本免费)

- **安全组 / 服务器防火墙(免费,主力)**:只放行 frp 的非标端口;SSH 限制来源 IP;其余全关。注意这是端口级 ACL,只管"端口对谁开",**不管进来的人是不是你**。
- **DDoS 基础防护(免费,默认开,最高约 5Gbps)**:挡常规流量型 DDoS。注意黑洞机制——超阈值时阿里云会丢弃**你 IP 的全部流量**来保护机房,即你的服务也会短时不可用。
- **WAF / 云防火墙 / 云安全中心**:基本都要付费,个人自用一般不需要。

> 关键:阿里云**没有任何"登录认证后才放行"的免费能力**。最接近的安全组也只是按 IP/端口放行。身份这层必须自建。

### 4.2 身份层 —— 自建(替代 Zero Trust)

推荐 **mTLS(双向 TLS)客户端证书**,自用、设备少时性价比最高:

- 普通 HTTPS 是单向:只有服务器证明身份,谁都能连。
- mTLS 是双向:**客户端也得出示证书**,服务器验证通过才握手。没有合法证书的人,**TLS 握手第一步就被踢掉**,连 jarvis 登录页都看不到。
- 实现:你自己当一个小型 CA → 给每台设备签一张客户端证书 → Caddy 配置成只信任你这个 CA → 只有装了证书的设备能进。
- 优点:无密码(不怕撞库/钓鱼)、拒绝发生在最外层、设备少时一劳永逸。
- 代价:首次每台设备导入证书略麻烦;丢设备要吊销该证书;加新设备要重新签。

> 其它可选替代:Caddy Basic Auth(最简,账号密码);Authelia / oauth2-proxy(最接近 Cloudflare Access,带 SSO + 2FA,但要多跑一个服务);WireGuard(干脆不公开暴露,安全最高但每台设备要先连 VPN)。

**最终组合 = 阿里云网络层(安全组 + DDoS 基础)+ 自建身份层(mTLS),两层叠起来约等于过去 Cloudflare 一家提供的保护。**

---

## 5. 实施路线(分阶段)

### 阶段一:打通隧道(先能用)

1. 买**阿里云大陆轻量应用服务器**(选固定公网 IP)。安全组放行 frp 端口(如 7000)和对外端口(如 8443),SSH 限来源 IP。
2. 服务器装 **frps**,配置 `bindPort` + `auth.token`(随机密钥),做成 systemd 开机自启。
3. 本地机器装 **frpc**,把 `127.0.0.1:<jarvis端口>` 通过 TCP 转到 frps,同样做成开机自启、断线重连。
4. 验证:`http://<公网IP>:<端口>` 能访问到 jarvis。

### 阶段二:HTTPS + 身份认证(加固)

5. (可选)买域名,A 记录解析到服务器 IP,**不备案**。
6. 服务器装 **Caddy**,反代到 frp 转上来的本地端口;用 **DNS-01 验证**(阿里云 DNS 插件)自动签 Let's Encrypt 证书,监听非标端口如 `:8443`。
7. 在 Caddy 上启用 **mTLS**:建自有 CA,给每台设备签客户端证书并导入(手机装 .p12,浏览器导入证书)。
8. 验证:授权设备能正常访问;未装证书的设备握手失败。

### 阶段三(未来):自建安卓 App + 设备绑定

把 mTLS 客户端从"浏览器 + 系统证书"升级成一个**只装在固定设备上的私有 App**,等于一张"长在手机里、拔不出来的门禁卡"。

- **形态**:若 jarvis 前端是网页,App 可做成 **WebView 壳** —— 壳负责 mTLS + 密钥管理 + 生物识别这层门禁,内部加载服务器网页。开发量小。
- **私钥放哪(决定"只在固定设备能用"是否成立)**:
  - **不要**把私钥硬编码进 APK(可被解包提取、复制到别的设备)。
  - **正确做法**:用 **Android Keystore(硬件级 TEE / StrongBox)**,App 首次运行在安全硬件内生成密钥对,**私钥不可导出**,只把该设备公钥在服务器授权一次。"只有这台物理设备能进"由硬件强制保证。
  - 再叠 **生物识别解锁**(指纹/人脸)作为 "something you are",丢手机别人也用不了。
- **分发**:APK 只 sideload 到自己设备(或 Play 私有测试轨道),不公开上架。
- **代价**:从此要维护一个 App;换设备需重新授权;临时借别人设备用不了(也正是其安全点)。

> 这一步等于自建一个**绑定物理设备的私有 Zero Trust 客户端**,个人自用场景下安全性比 Cloudflare Access 只强不弱,且整条链路完全自己掌控。

---

## 6. 成本

| 项目 | 费用 | 说明 |
|---|---|---|
| 阿里云大陆轻量服务器 | ~300 元/年起 | 30M 带宽对文本/API 流量绰绰有余;选固定公网 IP |
| 域名(可选) | ~50 元/年 | 只做解析、不备案;纯 IP 方案可省 |
| HTTPS 证书 | 免费 | Let's Encrypt,走 DNS-01 验证 |
| DDoS 基础 / 安全组 | 免费 | 阿里云自带 |
| **合计** | **约 300–350 元/年** | 无其它隐性费用 |

---

## 7. 待确认事项(影响最终配置)

1. **jarvis 本地监听端口**是多少?(写 frpc 配置要用)
2. **是否买域名**,还是直接用 IP?(决定 HTTPS 方案:正规证书 vs 自签)
3. **你的主力运营商**(电信/联通/移动)?电信走香港会绕日本,但本方案是大陆服务器,三网直连,基本无此问题——仅作记录。
4. **要授权几台设备**?(决定一开始签几张客户端证书)

---

## 附:与香港方案的对比(为何最终选大陆)

| | 大陆服务器 + 非标端口(推荐) | 香港轻量服务器 |
|---|---|---|
| 备案 | 免(靠非标端口) | 免(靠地域) |
| 国内延迟 | 低,三网直连 | 较高,电信晚高峰可能绕日本丢包 |
| 链路跨境 | 全程境内,无跨境 | frp 隧道双跨境(大陆↔香港来回两次) |
| 价格 | ~300 元/年 | ~288 元/年 |
| 结论 | **延迟更低、更稳,同样免备案** | 仅在不愿用非标端口时才考虑 |
