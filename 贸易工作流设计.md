# 贸易工作流范式 · 账户生命周期与贾维斯配合

> 定义「潜客 → 开发 → 收 list → 整理筛选 → 分发 → 议价 → 成交 → 复购」整条业务链的
> **对象模型、状态机、节律规则**，以及贾维斯在每一环的介入方式。
> 版本 **v0.1（设计）** · 2026-07-21

---

## 0. 一句话结论

**你现在描述的是一条流水线，但你的生意其实是一个循环。**

流水线有终点：成交，或没成交。循环没有终点：一家给过 excess list 的公司，
三个月后一定还会有新的余料——你和这家的关系，才是资产；单次交易只是这份资产
的一次提取。

你自己指出最大痛点是「客户开发进程和周期管理」，根因就在这里：**线性框架里没有
"周期"这个概念的位置**。一条从左流到右的管道，天然会把所有非成交结局都当作
"流出管道"，于是每一次未成交都是一次资产销毁。改成状态机 + 唤醒节律之后，
未成交只是"回到池子里等下一轮"。

本文档的全部设计都从这一句展开。

---

## 1. 你的描述里缺的七个环节

按链条顺序列出。这些不是理论遗漏，每一条都对应一个具体的漏水口。

### 1.1 联系人层缺失（公司 ≠ 人）

你说"先确认公司"，然后"冷开发邮件"。中间跳过了一步：**发给谁**。

同一家公司里，采购经理、库存/仓储经理、财务、总经理对"处理呆滞库存"这件事的
动机完全不同——库存经理背的是库存周转 KPI，excess 是他的痛点；采购经理背的是
成本，excess 是他过去的失误证据。发错人不是回复率低一点的问题，是方向反了。

**缺的对象：Contact。** 一家 Account 下面可以有多个 Contact，各自有独立的
触达记录和回复状态。"这家公司没回复"和"这家公司的采购总监没回复、还没试过
库存经理"是两件事，前者该归档，后者该换人再打一次。

### 1.2 "有没有合作机会"是个二值判断，但现实是光谱

你写「如果得到客户的回复有合作机会则进入下一阶段」。实际回复至少有五种：

| 回复类型 | 你现在的处理 | 应该的处理 |
|---|---|---|
| 有余料，愿意发 list | 进下一阶段 ✅ | 同左 |
| 有兴趣，但当前没余料 | 大概率就这么散了 ⚠️ | **入培育池，设唤醒日** |
| 有兴趣，但要走内部流程/需要 NDA | 容易卡死 ⚠️ | 卡在这一步要有超时提醒 |
| 已有合作渠道 | 当拒绝 ⚠️ | 记下**对手是谁**，设长周期唤醒 |
| 明确不做 | 拒绝 ✅ | 记原因，进黑名单（防重复触达） |

中间三种是你现在最大的泄漏面。它们不是失败，是**时机不对**——而时机是会变的，
前提是你还记得这家、并且知道什么时候该再问一次。

### 1.3 "无余料"回复没有归宿（最大的单点泄漏）

单独拎出来，因为这是量最大的一类。

一家愿意回你邮件、承认自己有呆滞库存问题、只是当下没货可出的公司，其价值
**高于一个全新的冷线索**——它已经通过了最难的两道筛（找对人、建立信任）。
线性流程会把它和"没回复"归到同一个结局：没有下一步。

正确处理：进 **S8 培育池**，带一个明确的 `wake_at` 日期（默认 60–90 天，
或客户自己说的时点，如"下季度盘库后"）。到期由贾维斯推到你的日程上。

### 1.4 大 list 的"未发出部分"去了哪里

你说大量的 list 会筛出最高价值的一批作为首批。那第二批呢？

如果首批反馈好，第二批应该在几天内跟上；如果首批没人要，剩下的更该重新评估
而不是无限期搁置。现在这部分**没有排期机制**，大概率就沉在某个文件夹里了。

**缺的对象：Lot（一批 list）与 Line（料号行）的分离。** 一个 Lot 里的 Line
有各自独立的状态：`待评估 / 已入选首批 / 已分发 / 有报价 / 议价中 / 成交 /
流拍 / 未发出`。"这批 list 处理完了吗"这个问题，只有 Line 级状态才能回答。

### 1.5 买家的沉默是数据，现在是死数据

发给买家网络的型号，有人报价、有人不报价。**没人报价的型号本身就是极强的
市场信号**——它告诉你这个型号/这个品类当下没有需求。

现在这个信息随邮件线程一起沉底了。下次同样的型号从另一家客户的 list 里冒出来，
你还得重新判断一次。

**缺的机制：料号级的报价与流拍记账。** 每个型号累积「问过几次 / 几次有报价 /
报价区间 / 最终成交价」，这份数据每天都在自己增值，且直接回答你的选品问题。

### 1.6 成交之后没有复购触发器

成交是你的流程终点，但对这家客户来说，那是**信任建立完成的时刻**——此时
索取下一批 list 的成功率是全链路最高的。

成交后应当自动产生两个未来事件：一次短期的（交付后 2 周，确认体验 + 问下一批）、
一次周期性的（按该客户的盘库节奏，30/60/90 天再问）。

### 1.7 价格信息没有单一事实源

双边议价时，卖家的期望价、买家的首轮报价、你的报出价、还价、最终成交价，
现在全部散在邮件线程里。这在同时跑多个 Lot 时是**报错价的直接风险**，
而且事后无法复盘"我在哪一类型号上让价太快"。

**缺的对象：Quote（报价记录）**，挂在 Line 上，双边分别记账。

---

## 2. 对象模型（五层）

```
Account   公司/供应商（长期资产，永不"结束"）
  └ Contact   联系人（一家多个，各自独立触达状态）
  └ Lot       一批 excess list（一次交易机会，一家可有 N 个）
       └ Line      料号行（最小交易单元，独立状态）
            └ Quote     报价（side=seller/buyer，多条，带时间戳）
            └ Deal      成交（一个 Line 或若干 Line 的组合）
Part      料号主数据（跨 Lot 跨客户累积：历史报价、流拍次数、成交价）
```

**关键分离：Account 与 Lot 必须分开。** 你的描述里这两者是混着的——
"开发一个客户"和"处理一批 list"被当成了同一条流程的前后段。分开之后：

- Account 有自己的生命周期（下一节的状态机），以**月/季**为周期
- Lot 有自己的处理流程，以**天/周**为周期
- 一个 Account 可以同时挂着"上一批 Lot 在议价中"和"下一批 Lot 已排期唤醒"

这也是"进程和周期管理"能成立的前提：**两个不同时间尺度的东西，不能用一套
进度条表示。**

**Part 是唯一跨账户累积的对象**，也是本设计里唯一会随时间自动增值的资产。

---

## 3. Account 状态机（核心）

```
S0 未识别
 └→ S1 已识别（潜客名单产出，现有 prospect_daily 的终点）
     └→ S2 已验证（画像确认 + 找到具体 Contact）
         └→ S3 已触达（首封冷邮件已发）
             ├→ 无回复 → F1/F2/F3 跟进 → 仍无 → S7 静默池
             └→ S4 已回复
                 ├→ S4a 有余料意向 → S5 已索取 list → S6 已收到 list ──┐
                 ├→ S4b 有兴趣·当前无货 ────────────────→ S8 培育池   │
                 ├→ S4c 卡流程（NDA/审批）→ 超时提醒 → S5 或 S8       │
                 └→ S4d 明确拒绝 ─────────────────────→ S9 冷冻      │
                                                                      │
S6 → S10 已整理 → S11 已分发买家 → S12 收到买家报价 → S13 议价中 ─────┤
                                                                      │
                    S14 成交 ──┐                                      │
                    S15 流拍 ──┴──────────────────────→ S8 培育池 ←───┘
```

### 三条不变量

1. **除 S9（明确拒绝）外，所有终态都回流 S8 培育池，并带 `wake_at`。**
   没有"结束"，只有"下一次什么时候问"。
2. **S7 静默池不是死亡。** 90 天后允许换 Contact / 换角度重启到 S3，
   最多重启两次，第三次仍无回复才进 S9。
3. **状态迁移必须带时间戳和原因。** 没有迁移历史，"周期管理"退化成一张
   当前状态快照，答不了"这家卡在议价多久了"。

### SLA：每个状态的最长停留天数

这是「周期管理」的实质——不是记录状态，是**记录状态的年龄**。

| 状态 | 正常时限 | 逾期动作 |
|---|---|---|
| S3 已触达 | 5 天 | 触发 F1 跟进提醒 |
| F1 → F2 | 7 天 | 触发 F2 |
| F2 → F3 | 14 天 | 触发 F3（最后一封，换角度） |
| S5 已索取 list | 7 天 | 提醒催 list |
| S6 已收到 list | 2 天 | 提醒：list 还没整理 |
| S10 已整理 | 2 天 | 提醒：整理完了还没发买家 |
| S11 已分发 | 5 天 | 提醒：买家没反应，考虑换池/降价 |
| S13 议价中 | 7 天 | **最高优先级告警**（议价卡住＝最贵的停滞） |
| S8 培育池 | 至 `wake_at` | 到期推入当日待办 |

> **逾期不是错误，是待办。** 系统只负责让它浮出来，不自动做任何事。

---

## 4. Lot / Line 处理流程

```
Lot 收到（原始文件，格式五花八门）
  ↓ ① 归一化（intake）
标准化表：料号 / 品牌 / 数量 / 批号 / 封装 / 日期码 / 备注
  ↓ ② 料号识别与去噪（对齐 Part 主数据）
  ↓ ③ 估值与打分
每行带：参考价来源、历史流拍次数、买家近期询价热度、综合分
  ↓ ④ 分批（小 list 全发 / 大 list 切首批 + 第二批排期）
  ↓ ⑤ 分发（你自己发，系统只出清单与话术素材）
  ↓ ⑥ 报价回收 → 写入 Quote + 回写 Part 主数据
  ↓ ⑦ 议价（双边价格看板）
  ↓ ⑧ 成交 / 流拍 → 两者都回写 Part，都触发 Account 回流 S8
```

**② 和 ⑥ 是全流程里最该自动化的两步**，因为它们纯机械、且每做一次数据资产就
增厚一层。③ 的规则由你定，系统只负责稳定执行 + 事后回测。

### 选品打分（把"经验"显性化）

你说选品靠市场行情、个人经验、买家首轮报价三者。前两者可以固化：

```
score = w1 × 参考价位阶      （查到的市场价段位）
      + w2 × 品类热度        （近 90 天该品类买家询价/报价频次）
      + w3 × 数量适配度      （数量落在好出手的区间，太少太多都扣分）
      + w4 × 品牌流通性      （历史成交率）
      - w5 × 历史流拍惩罚    （这个型号问过 N 次没人要）
```

权重由你给，第一版全部拍脑袋没关系——**关键是每次成交/流拍后回测**：
被打高分却流拍的、被打低分却高价成交的，都进一份"打脸清单"，你据此调权重。
三个月后这套权重就不再是猜的了。

> 第三个依据（买家首轮报价）本质上是**事后**信息，不能用于事前筛选。
> 但它必须被记账——它是 w2 和 w5 的数据来源。这就是 §1.5 那笔死数据的用途。

---

## 5. 贾维斯的配合方式

前提：**贾维斯不碰邮件收发。** 所有对外沟通由你亲自完成。这划定了清晰的边界——
贾维斯是**内部的记账员、调度员和分析员**，不是代理人。

但要注意区分：**不代发 ≠ 不感知**。状态推进需要输入，三条通道：

1. **对话直说**：「XX 公司回了，说下季度盘库后再联系」→ 贾维斯改状态、设唤醒日
2. **粘贴邮件正文**：贾维斯解析出意图、公司、承诺时点，提议状态迁移（你确认）
3. **导入文件**：excess list 文件丢进来 → 走 intake 流程

### 5.1 贾维斯要做的五件事

| # | 能力 | 对应你的痛点 |
|---|---|---|
| 1 | **每日节律扫描（account_pulse）** | 客户开发进程与周期管理 |
| 2 | **状态机记账与迁移** | 现有客户管理 |
| 3 | **list 归一化与打分（list_intake）** | list 整理与筛选 |
| 4 | **Part 价格与流拍主数据库** | 选品依据的沉淀 |
| 5 | **双边议价看板** | 防报错价 + 事后复盘 |

### 5.2 每日节律扫描 —— 最先该做的一件

每天早上产出一份**当日行动清单**，只回答一个问题：*今天该碰哪几家、为什么。*

```
【逾期·高】议价卡住 9 天    Nordwind GmbH   Lot#12  待你回买家 4.2 USD 的还价
【逾期·中】list 已收 4 天未整理   Vertex EMS   Lot#15（238 行）
【今日唤醒】培育池到期 3 家
   · Kessler Automation  60 天前说"Q3 盘库后再谈"，今天是约定时点
   · Baltic Circuits     上次成交后 90 天
   · Elmec Srl           上次回复"暂无余料"，到期复问
【跟进】F2 到期 5 家（首封 12 天前发出，未回复）
【新线索】昨日潜客名单 top 8 待触达
```

这份清单把「进程和周期管理」从一件需要你记在脑子里的事，变成一件每天早上
花三分钟读完的事。**它是本设计里最高性价比的单点交付。**

### 5.3 映射到现有架构（尽量不造新东西）

你的架构里已经有这套东西需要的全部机制，逐条对应：

| 需求 | 用现有的什么 | 说明 |
|---|---|---|
| Account/Contact/Lot/Line/Part 存储 | **`core/entities.py`**（`kind` + `fields` JSON） | 通用 kind 模型天生够用：`kind=account/contact/lot/line/part`。**起步阶段绝不新建表** |
| 状态迁移历史 | `entities.fields` 里一个 `history[]` 数组 | 单用户量级，不值一个独立存储层 |
| `wake_at` 唤醒日期 | **`core/calendar.register_source`** | 注册一个 provider，培育池到期自动出现在 `agenda()`——**这是现成机制，与"周期管理"完美契合** |
| SLA 逾期检测 | 同上，另一个 provider（读时现算） | 逾期是派生数据，不入表，符合日历"真源 vs 派生"的既有划分 |
| 每日行动清单 | **`core/intel_cards.register_card`** | 注册 3–4 张卡：逾期跟进 / 今日唤醒 / 议价中 / 待整理 list。空卡自动隐藏的现有行为正好适用 |
| 定时触发 | **`core/schedule_presets.PRESETS`** | 加一条 `account_pulse` 预设，前端一键启用 |
| list 归一化 / 打分 | **`core/workflow.py` 工作流** | 有序 Step + checkpoint 落盘，与 `prospect_daily` 同构 |
| 料号价格查询 | 自建技能（`tool_builder`） | 查行情平台的抓取类工具，正是 `env_probe` 擅长的场景 |

**一条都不需要新的存储层。** 这与你在 v0.5 删掉潜客历史库的判断同源——
「重复率撑不起一个存储层」。同理：单用户的账户状态机，撑不起第二个 SQLite 表族。
量级过千再抽独立表，届时 `entities` 里的数据结构已经被真实使用打磨过了。

### 5.4 与现有 prospect_daily 的接缝

现有 `prospect_daily` 工作流的终点是**一份 xlsx + 情报台"今日名单"**。
本设计的起点 S1 正好接在这里：

```
prospect_daily 出名单（S0 → S1）
      ↓  你从名单里挑几家决定要打
   建 Account 实体，状态 = S2   ← 这一步是新增的、也是唯一的接缝
      ↓
   本文档的状态机接管
```

**接缝要保持窄。** 名单是队列（跑完就该换新的），Account 库是资产（永不清空）——
这两者的性质差异，和你在契约 §5 里区分「队列 vs 快照」是同一个道理。
不要让 Account 库反过来影响潜客名单的生成或排序，那会重蹈机会轨混进覆盖轨的覆辙。

---

## 6. 分期落地

按"先解决最痛的、且不依赖任何外部集成的"排序。

### Phase 1 · 状态机与节律（最高优先，纯内部）

1. `core/accounts.py`：状态机定义 + 迁移函数 + SLA 表（数据落 `entities`）
2. 三个工具：`account_update`（迁移状态）、`account_brief`（查一家全貌）、
   `account_due`（列出所有逾期/到期）
3. `calendar.register_source("account_wake", ...)`：唤醒日与逾期入日程
4. 情报台注册 3 张卡：逾期跟进 / 今日唤醒 / 议价中
5. `schedule_presets` 加 `account_pulse` 预设（每工作日早 8:30）

> Phase 1 做完，你的核心痛点（开发进程与周期管理）就已经被覆盖了。
> 后面两期是效率提升，这一期是止血。

### Phase 2 · list 处理与料号主数据

6. `list_intake` 工作流：任意格式 excess list → 标准化表（复用 `read_document`）
7. `kind=part` 主数据：料号归一、报价记账、流拍计数
8. `quote_log` 工具：双边报价录入 + 议价看板渲染

### Phase 3 · 选品打分与回测

9. 打分函数（权重可配）+ 分批建议（首批/第二批排期）
10. 回测报告：注册一个 `core/reports` 报告类型，月度输出"打脸清单"与权重建议

---

## 7. 有意不做的事

明确记下来，防止后来被"顺手加上"：

- **不代发任何邮件。** 边界一旦破，"内部记账员"的角色定位就没了，
  而且状态机会开始依赖发信成功与否，可靠性直接下一个台阶。
- **不维护买家库、不做买家匹配。** 买家网络不归你维护——把它当外部黑盒，
  只记录"发出去了什么、回来了什么价"。越过这条线会重复造别人已有的东西。
- **不给 Account 算综合评分。** 理由同契约 §4 反对合成 `weight`：
  把状态年龄、成交历史、confidence 乘成一个小数是**假精度**。
  多级排序 + 逾期天数摊开显示，你一眼能看懂、能自己重排。
- **不自动改状态。** 所有迁移由你确认（对话里一句话即可）。
  自动推断的状态一旦错了，错误会沉默地一路传染到唤醒调度。

---

## 8. 待你确认的三件事

1. **SLA 天数**（§3 表格）是我按常见 B2B 节奏拍的，你实际的节奏是什么？
   这几个数字直接决定每日清单的信噪比——定太紧会天天满屏告警，定太松等于没有。
2. **培育池默认唤醒周期**：60 天？90 天？是否按客户规模/品类分档？
3. **打分权重**（§4）的初版取值，以及"参考价"到底查哪几个源
   （这决定 Phase 3 要造什么抓取工具）。
